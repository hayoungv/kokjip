# 저장소 안에서는 LF 로 통일한다.
* text=auto eol=lf

# 셸 스크립트는 CRLF 면 실행 자체가 깨진다.
*.sh text eol=lf

# 바이너리 — 변환 금지
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.ico binary
*.xlsx binary
*.woff binary
*.woff2 binary
