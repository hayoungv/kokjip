# 작업 규칙 (SSOT)

이 문서가 저장소의 단일 기준이다. 다른 문서와 충돌하면 이 문서를 따른다.

---

## 1. 문서 계층과 순서

| 순서 | 경로 | 답하는 질문 | 선행 조건 |
|---|---|---|---|
| 1 | `docs/00-plan/` | 착수 전에 무엇을 정해야 하는가 | 입력 문서 확인 |
| 2 | `docs/01-prd/` | 왜 만드는가 · 누구를 위한 것인가 | 00-plan 결정 완료 |
| 3 | `docs/02-srs/` | 무엇을 만족해야 하는가 | PRD 확정 |
| 4 | `docs/03-tds/` | 어떻게 설계하는가 | SRS 확정 |
| 5 | `tasks/` | 어떤 단위로 구현·검증하는가 | TDS 확정 |

**앞 단계를 건너뛰고 뒤 단계를 쓰지 않는다.** 뒤 단계에서 앞 단계의 결정을 바꿔야 하면, 앞 문서를 먼저 고치고 그 변경을 뒤로 전파한다.

---

## 2. 문서 작성 규칙

- 같은 사실을 두 문서에 복사하지 않는다. 필요하면 원본 위치만 가리킨다.
- 파일명은 ASCII 소문자 kebab-case를 쓴다. 버전은 파일명이 아니라 문서 헤더에 적고 "버전"이라고 쓴다. "판"이라고 쓰지 않는다.
- `docs/00-plan/references/`는 읽기 전용이다. 여기 있는 파일을 직접 고치지 않는다.
- 결정을 바꾸면 바꾼 이유를 결정 기록부에 남긴다.

---

## 3. PRD 작성 규칙

PRD는 **서비스를 처음 보는 사람**과 **이 서비스를 개발할 사람**이 같은 문서를 읽고 같은 것을 이해할 수 있어야 한다.

- 설명 없이 쓰는 약어·전문 용어를 넣지 않는다. 꼭 필요하면 처음 나올 때 한 문장으로 풀어 쓴다.
- 기호로 의미를 대신하지 않는다. 등급 표시, 화살표, 별표 같은 것으로 판단을 압축하지 않고 문장으로 쓴다.
- 외부 문서 링크를 본문에 넣지 않는다. 필요한 내용은 PRD 안에 직접 적는다.
- 작성자의 추측이나 의견을 넣지 않는다. 확정된 결정과 그 결정이 요구하는 것만 적는다.
- 한 화면에서 구조가 보이도록 쓴다. 목차를 따라가면 무엇을 만드는지가 끝까지 이어져야 한다.

---

## 4. 커밋 규칙

- Conventional Commits 형식을 쓴다. 예: `docs(prd): ...`, `chore: ...`, `fix(srs): ...`
- 의미 있는 작업 단위가 끝날 때마다 커밋한다. 여러 단계를 한 커밋에 몰지 않는다.
- 커밋 메시지 제목은 무엇을 왜 바꿨는지가 드러나게 쓴다.

---

## 5. 제품 확정 사항

착수 전 결정으로 확정된 것이며, 이후 작업은 이 내용을 다시 협상하지 않는다. 결정의 근거는 [`docs/00-plan/product-decisions.md`](docs/00-plan/product-decisions.md)에 있고, 남은 미해소 항목은 [`docs/00-plan/grip-register.md`](docs/00-plan/grip-register.md)에서 추적한다.

### 5-1. 고객 구조 (T1)

- 값을 지불하는 주체는 **훈련기관**이고, 제품을 직접 쓰는 주체는 **학습자**다.
- 기관 운영자용 화면과 학습자용 화면은 목적이 다르므로 하나로 합치지 않는다.
- 학습자 계정은 기관에 소속된다. 개인이 스스로 가입해 쓰는 경로를 만들지 않는다.
- 개인 단위 결제 흐름을 설계하지 않는다.

### 5-2. 기관이 얻는 값 (T2)

- 기관의 구매 이유는 1순위가 **중도 탈락 감소**, 2순위가 **취업률 방어**다. 기관용 화면과 지표는 1순위 축에 맞춘다.
- 제품의 효과 지표는 훈련 기간 안에 측정 가능한 것만 쓴다. 취업률은 상위 목적으로만 언급하고 효과 지표로 삼지 않는다.
- 강사별·과정별 품질 점수나 순위를 산출하지 않는다.
- 취업 결과와 학습 이력을 연결해 추적하는 기능을 설계하지 않는다.

### 5-3. 강의 음성의 획득과 보관 (T3)

- 강의 음성은 **학습자가 수업 당일 직접 올린다.** 기관이나 강사가 올리는 경로를 만들지 않는다.
- **서버는 강의 원본 음성을 보관하지 않는다.** 원본은 학습자 기기에만 두고, 서버는 전사 텍스트와 시각 정보만 갖는다.
- 다시 듣기는 기기의 원본 파일을 해당 시점으로 여는 방식으로 구현한다. 원본이 없는 경우의 화면 처리를 반드시 정의한다.
- 녹음 허용은 **제품이 강사에게 직접 받는다.** 기관을 거치지 않는다. 강사 동의 전에는 업로드 칸을 열지 않는다.
- 강사에게 회차 단위 차단 권한을 준다. 기본값은 열림이다.
- 회차는 기관이 등록하지 않는다. 업로드된 녹음의 날짜로 제품이 생성한다.
- 기관에 반복 업무를 남기지 않는다. 기관의 작업은 과정 개설·강사 연락처 입력·학습자 초대에서 끝난다.

### 5-4. 과금 단위와 계약 형태 (T4)

- 과금은 **그달에 실제로 다니고 있는 수강생 수**를 세어 **매월** 청구한다. 정원 기준 선불이나 기수당 정액을 쓰지 않는다.
- 단가는 **1인 1개월 3,000원**이다. 이 값은 기관 인터뷰로 확인해야 할 대상이며, 확인 전까지 다른 값으로 바꾸지 않는다.
- 이탈한 수강생은 이탈한 달까지만, 합류한 수강생은 합류한 달부터 청구한다.
- 계약 단위는 과정이다. 과정이 끝나면 계약이 닫히며 자동 갱신을 기본값으로 두지 않는다.
- 월별 재적 인원과 이탈·합류 시점은 청구의 근거가 되므로 제품이 기록한다.

### 5-5. 신호 판정의 품질 기준선 (T5)

- 신호 판정은 **정밀도를 우선**한다. 잘못 잡는 것이 놓치는 것보다 비싼 오류다.
- **재현율 88.7%를 하한으로 둔다.** 정밀도를 올리는 변경이라도 재현율이 이 값 아래로 내려가면 채택하지 않는다.
- 분류기를 고칠 때마다 정밀도와 재현율을 같은 평가셋·같은 지표 정의로 함께 측정하고 기록한다. 한쪽만 보고하지 않는다.
- 복습 목록과 별개로 **전사 전체를 열어 볼 수 있는 경로**를 반드시 둔다. 놓친 대목에 대한 안전망이다.
- 목록의 각 항목에는 근거가 된 강사 발화를 그대로 함께 보여준다.

### 5-6. 첫 버전의 기능 범위 (T6)

- 첫 버전에 넣는 것은 다음 여덟 가지다. 강사 동의, 강사의 회차 차단, 학습자 업로드, 전사, 복습 목록, 해당 시점 다시 듣기, 전사 전체 열람, 기관용 뒤처지는 학습자 목록.
- 과정 개설·학습자 초대·재적 인원 집계·청구서 발행은 **첫 버전에서 제품 기능으로 만들지 않는다.** 사람이 처리한다.
- 다음 버전으로 미루는 것: 가용 시간에 맞춘 분량 조절, 프로젝트 키워드 역검색, 음향 기반 판정, 기관 관리 화면 전체.
- 만들지 않는 것: 퀴즈·카드 생성, 강사별 평가 점수, 취업 결과 연계.
- 기관 화면은 **제품이 직접 가진 데이터로만** 만든다. 출석 기록은 제품에 없다.

### 5-7. 뒤처짐 판정 기준 (T9)

- **복습 목록 화면을 연 날이 3일 연속 없으면** 뒤처지는 것으로 판정한다.
- 연속된 날은 **강의가 올라온 날만** 센다. 강의가 없는 날은 세지 않는다.
- 업로드 여부나 다시 듣기 비율을 판정 기준으로 쓰지 않는다.
- 기관 화면에는 며칠째 열지 않았는지를 그대로 보여주고, 점수나 등급으로 바꾸지 않는다.

### 5-8. 개인정보와 저작권 운영 규칙 (T7)

- 질의응답을 포함해 **전부 글로 옮긴다.** **화자를 구분하지 않는다** — 누가 말했는지에 대한 정보를 만들지 않는다.
- 복습 목록에 오르는 것은 **강사의 말뿐**이다. 판정이 발화 내용으로 가린다. 수강생 발언으로 보이는 구간은 맥락으로만 남는다.
- 기관 화면에 전사 내용을 노출하지 않는다.
- 업로드 화면에서 질의응답이 함께 전사된다는 사실을 학습자에게 알린다.
- 전사본은 **과정 종료 후 6개월**까지 보관하고 그 뒤 삭제한다. 이 기간을 강사 동의서에 명시한다. 근거는 KDT 취업률이 훈련 종료 후 6개월 이후에 산정된다는 점이다.
- 원본 음성은 서버에 두지 않는다 (§5-3).
- 강사 동의서에 들어갈 항목: 녹음 허용 범위, 전사 대상, 보존 기간 6개월, 회차 단위 거부권.

### 5-9. 용어 표기 기준 (T8)

- 제품 문서 본문에는 **한국어 표기만** 쓴다. 영문 약어를 쓰지 않는다.
- 강사가 강조한 대목의 유형은 다음 여덟 가지로 부른다. 주의 · 실무 적용 · 핵심 원칙 · 요건 · 필수 행동 · 근거 · 견해 · 복습 대상.
- 요건은 **상태**를, 필수 행동은 **행동**을 가리킨다.
- 그 밖의 명칭은 풀어 쓴다. 신호는 **강사가 강조한 대목**, 앵커는 **해당 발화가 시작되는 지점**, 후보 풀은 **사람이 검토한 대상 목록**으로 쓴다. 가치 B라는 표기는 쓰지 않고 문장으로 풀어 쓴다.
- **코드에서 쓸 식별자는 PRD가 정하지 않는다.** SRS에서 위 한국어 표기와 짝지어 정의한다.

---

## 6. 기술 확정 사항

SRS 착수 전 결정으로 확정된 것이며, 이후 작업은 이 내용을 다시 협상하지 않는다. 결정의 근거는 [`docs/00-plan/product-decisions.md`](docs/00-plan/product-decisions.md)의 기술 결정 절에 있다.

### 6-1. 실행 형태 (S1)

- 학습자는 **휴대폰 설치형 앱**, 훈련기관과 강사는 **웹 화면**을 쓴다.
- 학습자용 기능을 웹으로 만들지 않는다. 브라우저 저장소에 녹음 원본을 두지 않는다.
- 강사 동의는 설치 없이 웹에서 끝나야 한다. 강사에게 앱 설치를 요구하지 않는다.
- 앱 저장소의 용량을 관리하는 방법을 학습자에게 제공한다.

### 6-2. 녹음 획득 방식 (S2)

- 녹음은 **앱이 직접** 한다. 다른 앱에서 만든 파일을 가져오는 기능을 만들지 않는다.
- 녹음 시작 시점에 강사 허용 여부를 확인한다. **허용되지 않은 수업은 녹음 버튼을 열지 않는다.**
- 긴 녹음은 일정 길이의 조각으로 나눠 저장한다. 중간 종료 시 그때까지의 조각이 남아야 한다.
- 화면이 꺼져 있거나 다른 앱을 쓰는 동안에도 녹음이 이어져야 한다.
- 저장 공간 부족은 녹음 시작 전에 알린다.

### 6-3. 음성의 서버 경유 (S3)

- 음성은 **글로 옮기는 동안에만** 서버에 존재한다. 옮기기가 끝나면 **즉시 지운다.**
- 옮기기에 실패해도 **24시간 안에** 지운다.
- 음성이 들어온 시각과 지워진 시각을 기록한다.
- 처리 중인 음성을 사람이 열어 듣지 않는다. 음성과 옮긴 글을 인공지능 학습에 쓰지 않는다.
- 앱이 외부 전사 서비스로 음성을 직접 보내지 않는다. 외부 서비스 접속 열쇠를 앱에 넣지 않는다.

### 6-4. 회차당 정본과 발화 지점 (S10)

- 한 회차에서 **옮기는 총량은 그 회차에 존재하는 구간의 합집합 한 벌**이다. 학습자 수만큼 옮기지 않는다.
- 그 회차의 **첫 업로드를 즉시 기준으로 삼는다.** 다른 업로드를 기다리지 않는다.
- 이후 업로드는 기준이 담지 못한 구간만 옮긴다. 이미 옮긴 구간을 다시 옮기지 않는다.
- 발화 지점은 **실제 시각으로 저장한다.** 특정 녹음 기준의 상대 시각으로 저장하지 않는다.
- 앱은 녹음의 시작·종료 실제 시각, 중단 구간, 서버 시각과의 차이를 함께 올린다.
- 학습자의 녹음이 그 지점을 담고 있지 않으면 다시 듣기를 열지 않고 그 사실을 알린다.

### 6-5. 전사 방식과 원가 상한 (S4)

- 전사는 **기기 안에서 조각 단위로 이어서** 한다. 앞선 조각의 전사가 뒤 조각의 녹음과 겹쳐 돈다. 서버로는 **글만** 올린다.
- 다른 앱으로 이동하거나 다른 앱이 만든 전사 결과를 가져오지 않는다. 운영체제가 제공하는 기기 안 인식 기능을 앱이 직접 부른다.
- 기기가 한국어 인식을 지원하지 않거나 실패한 경우에만 음성을 서버로 보낸다. 이 예외 경로에는 §6-3이 그대로 적용되며, 그 회차의 기준 한 벌에만 쓴다.
- 기기 내 처리를 **명시적으로 강제하는 호출**을 쓴다. 운영체제 기본 인식기는 음성을 제조사 서버로 보낼 수 있으므로 쓰지 않는다. 기기 우선 설정은 선호일 뿐 보장이 아니다.
- **원가 상한은 1인 월 1,000원**이다. 넘으면 예외 경로의 처리 방식을 바꾼다.
- §5-5의 재현율 하한 88.7%는 **이미 기기 전사본에서 나온 값**이다. 기기 전사로 바꾼다고 다시 재지 않는다. 재야 할 쪽은 클라우드 예외 경로다.
- 과정의 기술 용어 목록을 인식기에 힌트로 넘긴다. 깨지는 것이 정확히 그 용어들이다.

### 6-6. 처리 완료 시점 (S5)

- 복습 목록은 **올린 뒤 한 시간 안에** 보여준다. 이것이 상한이다.
- 판정은 **묶음으로 보낸다.** 판정비가 절반이 되며, 이 시점 요구가 그것을 가능하게 하는 근거다.
- 목록이 준비되면 학습자에게 알린다.
- 회차 전체를 기다리지 않는다. **올라온 만큼 먼저 처리해** 그 구간의 목록을 먼저 보여준다.

### 6-7. 계정과 인증 (S6)

- **학습자** — 초대 링크로 앱을 열고 **구글 계정으로 로그인**한다. **초대 명단에 있는 이메일로 로그인한 사람만** 들어온다. 문자 인증이나 간편인증을 쓰지 않는다.
- **강사** — 계정을 만들지 않는다. 동의 안내에 담긴 **본인 전용 주소**가 열쇠이며, 동의와 회차 차단을 같은 주소에서 한다. 주소는 추측할 수 없어야 하고 유효 기간이 있다.
- **기관 담당자** — 이메일과 비밀번호.
- 학습자에게 가입 절차를 만들지 않는다. 강사에게 가입을 요구하지 않는다.
- 한 학습자의 소속은 하나다. 동시 복수 소속은 첫 버전에서 다루지 않는다.

### 6-8. 복습 목록의 단위 (S5 보강)

- 복습 목록은 **하루에 하나**다. 녹음이 쉬는 시간과 점심으로 여러 조각이 되어도 목록은 나누지 않는다.
- 조각이 올라올 때마다 그 구간의 결과를 같은 목록에 더한다. 하루가 끝나기를 기다리지 않는다.
- 판정할 때 **앞 조각의 끝부분과 뒤 조각의 앞부분을 문맥으로 함께 넣는다.** 조각 경계에 걸친 설명이 잘리지 않게 한다.
- 쉬는 시간 잡담을 따로 걸러내는 기능을 첫 버전에 만들지 않는다. 판정 규칙이 걸러내며, 앱은 **멈춤 버튼을 눈에 잘 띄게** 둔다.

### 6-9. SRS 문서 규약 (S8)

- 요구사항 번호는 `REQ-FUNC-001` · `REQ-NF-001` · `REQ-DATA-001` 형식을 쓴다. 한 번 붙인 번호는 바꾸지 않고 재사용하지 않는다.
- 요구사항 한 건에 **근거 · 내용 · 수용 기준** 세 가지를 붙인다. 근거에는 PRD 장 번호와 결정 번호를 적는다.
- 수용 기준은 **확인할 수 있는 문장**으로 쓴다. 확인할 수 없으면 수용 기준이 아니다.
- 본문 용어는 PRD의 한국어 표기를 그대로 쓴다.
- **코드 식별자는 부록의 표 한 곳에서만** 정의하고 본문에 흩지 않는다.
- 국제 표준 적합성 표는 넣지 않는다.

### 6-10. 확정 기술 스택 (S7)

**확정 스택**

| 레이어 | 선택 |
|---|---|
| 서버와 웹 화면 | Next.js App Router 단일 풀스택 |
| 언어 | TypeScript 엄격 설정 |
| 학습자 앱 | React Native (Expo) |
| 녹음과 기기 전사 | 각 운영체제 기능을 직접 부르는 별도 조각 (안드로이드·아이폰 각각) |
| 데이터베이스 | Prisma + Supabase PostgreSQL |
| 입력 검증 | Zod |
| 강조한 대목 판정 | Claude `claude-opus-5`, **묶음 호출** |
| 묶음 작업 예약 | Supabase `pg_cron` |
| 화면 꾸미기 | 웹은 Tailwind + shadcn/ui, 앱은 NativeWind |
| 알림 | 휴대폰 푸시 알림 |
| 테스트 | Vitest · Playwright · Maestro |
| 학습자 로그인 | 구글 계정 로그인 |
| 이메일 발송 | 무료 구간의 발송 서비스 |
| 인프라 | Vercel + Supabase **무료 구간** |

**금지 스택**

- 별도 백엔드 서버
- 운영체제의 기본 음성 인식기 — 제조사 서버로 보낼 수 있다
- 클라우드 전사를 기본 경로로 쓰는 구성 — 예외 경로에만 쓴다
- 서버에 원본 음성을 남기는 어떤 구성
- 앱에 외부 서비스 접속 열쇠를 넣는 구성
- 화자 분리
- 위치 정보를 다루는 부품
- 학습자 개인 결제 부품
- 문자 인증과 간편인증 — 공급자 계약과 기관 심사가 붙고 건당 비용이 든다

**판정은 반드시 묶음으로 보낸다.** 낱개로 보내면 1인당 1,073원으로 원가 상한을 넘는다.

**고정비를 두지 않는다.** 판정 외에 분량이나 사용자 수에 비례해 돈이 나가는 부품을 넣지 않는다. 인프라와 로그인과 이메일 발송은 무료 구간 안에서 해결한다.

### 6-11. 만드는 순서 (S11)

- **1단계 웹** — 전사본과 음성 파일을 올리는 지점부터 시작한다. 판정, 복습 목록, 지점 재생, 전사 전체 보기, 강사 동의와 회차 차단, 기관 화면까지 웹에서 만든다.
- **2단계 안드로이드 앱** — 녹음과 기기 전사를 더한다. 1단계에서 만든 서버와 창구를 그대로 부른다.
- 두 단계가 **같은 서버**를 쓴다. 웹 단계는 임시 시연물이 아니라 제품의 앞부분이므로 버리는 작업이 없어야 한다.
- 아이폰은 MVP 범위 밖이다.

### 6-12. SRS 다이어그램 (S9)

SRS에 넣는 그림은 **일곱 개**다. 이름표는 모두 한국어로 쓴다.

| 그릴 것 | 형식 |
|---|---|
| 시스템 맥락도 | 가로 흐름도 |
| 전사 경로 분기 | 세로 흐름도 |
| 녹음부터 복습 목록까지 | 시퀀스도 |
| 강사 동의와 녹음 열림 | 시퀀스도 |
| 발화 지점 시각 환산 | 간트 차트 |
| 개체 관계도 | 개체 관계도 |
| 뒤처짐 판정 흐름 | 세로 흐름도 |

- 고르는 기준은 **그 그림이 없으면 글로 설명하기 어려운가**다. 형식을 갖추기 위한 그림을 넣지 않는다.
- 유스케이스도·클래스도·컴포넌트도·상태 전이도·검증 전략 흐름도·외부 시스템 격리 경계도는 **넣지 않는다.**
- **만드는 단계를 그림에 섞지 않는다.** 대신 요구사항 목록에 단계 칸을 두어 1단계 여부를 표시한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

### 6-13. 확정 문구

바깥에 내보이는 문장은 [`docs/00-plan/copy.md`](docs/00-plan/copy.md)에 있다. **글자 그대로 쓴다.**

- 카피 문구: 강의에서 다시 볼 것만 콕.
- 서비스 설명: 콕집은 매일 쌓이는 강의를 처음부터 다시 볼 필요 없이, 지금 다시 봐야 할 내용만 골라 필요한 부분부터 효율적으로 복습할 수 있도록 도와주는 학습 서비스입니다.

줄이거나 바꿔 쓰지 않는다. 쓰지 않는 말의 목록도 같은 문서에 있다.

### 6-14. 강도

- 강조한 대목마다 **1에서 3 사이의 강도**가 붙는다. 클수록 강하게 강조한 것이다.
- 복습 목록은 **강도가 높은 순**으로 보여준다. 강도가 같으면 시각 순이다.
- 시각 순으로만 쌓지 않는다. 그러면 강의를 순서대로 훑는 것이지 먼저 볼 것을 고른 것이 아니다.

---

## 7. 문서를 고칠 때

**가장 최신 내용만 남긴다.** 변경 이력을 문서 본문에 남기지 않는다.

| 남긴다 | 지운다 |
|---|---|
| 왜 이 결정인가 | 언제 무엇이 바뀌었는가 |
| 다른 안을 택하지 않은 이유 | 이전 버전이 무엇이었는가 |
| 결정이 요구하는 것 | 재개방·수정·반영 이력 |

문서 헤더의 버전 번호는 현재 상태이므로 유지한다. 변경 이력이 필요하면 깃 커밋 메시지에 남긴다.

### 7-1. 용어

개발용으로 미리 넣어 두는 자료는 **seed**라고 부른다. "씨앗 데이터"처럼 옮겨 쓰지 않는다.

`prisma/seed.ts`는 지어낸 예시(기관·과정·강사·학습자)를 넣고, `prisma/seed-signals.ts`는 실측 판정 결과 53건과 실제 강의 전사본을 넣는다.
