# 작업 규칙 (SSOT)

이 문서가 저장소의 단일 기준이다. 다른 문서와 충돌하면 이 문서를 따른다.

---

## 1. 문서 계층과 순서

| 순서 | 경로 | 답하는 질문 | 선행 조건 |
|---|---|---|---|
| 1 | `docs/00-plan/` | 착수 전에 무엇을 정해야 하는가 | 입력 문서 확인 |
| 2 | `docs/01-prd/` | 왜 만드는가 · 누구를 위한 것인가 | 00-plan 결정 완료 |
| 3 | `docs/02-srs/` | 무엇을 만족해야 하는가 | PRD 확정 |
| 4 | `docs/03-tds/` | 어떻게 설계하는가 | SRS 확정 |
| 5 | `tasks/` | 어떤 단위로 구현·검증하는가 | TDS 확정 |

**앞 단계를 건너뛰고 뒤 단계를 쓰지 않는다.** 뒤 단계에서 앞 단계의 결정을 바꿔야 하면, 앞 문서를 먼저 고치고 그 변경을 뒤로 전파한다.

---

## 2. 문서 작성 규칙

- 같은 사실을 두 문서에 복사하지 않는다. 필요하면 원본 위치만 가리킨다.
- 파일명은 ASCII 소문자 kebab-case를 쓴다. 버전은 파일명이 아니라 문서 헤더에 적는다.
- `docs/00-plan/references/`는 읽기 전용이다. 여기 있는 파일을 직접 고치지 않는다.
- 결정을 바꾸면 바꾼 이유를 결정 기록부에 남긴다.

---

## 3. PRD 작성 규칙

PRD는 **서비스를 처음 보는 사람**과 **이 서비스를 개발할 사람**이 같은 문서를 읽고 같은 것을 이해할 수 있어야 한다.

- 설명 없이 쓰는 약어·전문 용어를 넣지 않는다. 꼭 필요하면 처음 나올 때 한 문장으로 풀어 쓴다.
- 기호로 의미를 대신하지 않는다. 등급 표시, 화살표, 별표 같은 것으로 판단을 압축하지 않고 문장으로 쓴다.
- 외부 문서 링크를 본문에 넣지 않는다. 필요한 내용은 PRD 안에 직접 적는다.
- 작성자의 추측이나 의견을 넣지 않는다. 확정된 결정과 그 결정이 요구하는 것만 적는다.
- 한 화면에서 구조가 보이도록 쓴다. 목차를 따라가면 무엇을 만드는지가 끝까지 이어져야 한다.

---

## 4. 커밋 규칙

- Conventional Commits 형식을 쓴다. 예: `docs(prd): ...`, `chore: ...`, `fix(srs): ...`
- 의미 있는 작업 단위가 끝날 때마다 커밋한다. 여러 단계를 한 커밋에 몰지 않는다.
- 커밋 메시지 제목은 무엇을 왜 바꿨는지가 드러나게 쓴다.

---

## 5. 제품 확정 사항

착수 전 결정으로 확정된 것이며, 이후 작업은 이 내용을 다시 협상하지 않는다. 결정의 근거는 [`docs/00-plan/product-decisions.md`](docs/00-plan/product-decisions.md)에 있고, 남은 미해소 항목은 [`docs/00-plan/grip-register.md`](docs/00-plan/grip-register.md)에서 추적한다.

### 5-1. 고객 구조 (T1)

- 값을 지불하는 주체는 **훈련기관**이고, 제품을 직접 쓰는 주체는 **학습자**다.
- 기관 운영자용 화면과 학습자용 화면은 목적이 다르므로 하나로 합치지 않는다.
- 학습자 계정은 기관에 소속된다. 개인이 스스로 가입해 쓰는 경로를 만들지 않는다.
- 개인 단위 결제 흐름을 설계하지 않는다.

### 5-2. 기관이 얻는 값 (T2)

- 기관의 구매 이유는 1순위가 **중도 탈락 감소**, 2순위가 **취업률 방어**다. 기관용 화면과 지표는 1순위 축에 맞춘다.
- 제품의 효과 지표는 훈련 기간 안에 측정 가능한 것만 쓴다. 취업률은 상위 목적으로만 언급하고 효과 지표로 삼지 않는다.
- 강사별·과정별 품질 점수나 순위를 산출하지 않는다.
- 취업 결과와 학습 이력을 연결해 추적하는 기능을 설계하지 않는다.

### 5-3. 강의 음성의 획득과 보관 (T3)

- 강의 음성은 **학습자가 수업 당일 직접 올린다.** 기관이나 강사가 올리는 경로를 만들지 않는다.
- **서버는 강의 원본 음성을 보관하지 않는다.** 원본은 학습자 기기에만 두고, 서버는 전사 텍스트와 시각 정보만 갖는다.
- 다시 듣기는 기기의 원본 파일을 해당 시점으로 여는 방식으로 구현한다. 원본이 없는 경우의 화면 처리를 반드시 정의한다.
- 녹음 허용은 **제품이 강사에게 직접 받는다.** 기관을 거치지 않는다. 강사 동의 전에는 업로드 칸을 열지 않는다.
- 강사에게 회차 단위 차단 권한을 준다. 기본값은 열림이다.
- 회차는 기관이 등록하지 않는다. 업로드된 녹음의 날짜로 제품이 생성한다.
- 기관에 반복 업무를 남기지 않는다. 기관의 작업은 과정 개설·강사 연락처 입력·학습자 초대에서 끝난다.

### 5-4. 과금 단위와 계약 형태 (T4)

- 과금은 **그달에 실제로 다니고 있는 수강생 수**를 세어 **매월** 청구한다. 정원 기준 선불이나 기수당 정액을 쓰지 않는다.
- 단가는 **1인 1개월 3,000원**이다. 이 값은 기관 인터뷰로 확인해야 할 대상이며, 확인 전까지 다른 값으로 바꾸지 않는다.
- 이탈한 수강생은 이탈한 달까지만, 합류한 수강생은 합류한 달부터 청구한다.
- 계약 단위는 과정이다. 과정이 끝나면 계약이 닫히며 자동 갱신을 기본값으로 두지 않는다.
- 월별 재적 인원과 이탈·합류 시점은 청구의 근거가 되므로 제품이 기록한다.

### 5-5. 신호 판정의 품질 기준선 (T5)

- 신호 판정은 **정밀도를 우선**한다. 잘못 잡는 것이 놓치는 것보다 비싼 오류다.
- **재현율 88.7%를 하한으로 둔다.** 정밀도를 올리는 변경이라도 재현율이 이 값 아래로 내려가면 채택하지 않는다.
- 분류기를 고칠 때마다 정밀도와 재현율을 같은 평가셋·같은 지표 정의로 함께 측정하고 기록한다. 한쪽만 보고하지 않는다.
- 복습 목록과 별개로 **전사 전체를 열어 볼 수 있는 경로**를 반드시 둔다. 놓친 대목에 대한 안전망이다.
- 목록의 각 항목에는 근거가 된 강사 발화를 그대로 함께 보여준다.

### 5-6. 첫 버전의 기능 범위 (T6)

- 첫 버전에 넣는 것은 다음 여덟 가지다. 강사 동의, 강사의 회차 차단, 학습자 업로드, 전사, 복습 목록, 해당 시점 다시 듣기, 전사 전체 열람, 기관용 뒤처지는 학습자 목록.
- 과정 개설·학습자 초대·재적 인원 집계·청구서 발행은 **첫 버전에서 제품 기능으로 만들지 않는다.** 사람이 처리한다.
- 다음 버전으로 미루는 것: 가용 시간에 맞춘 분량 조절, 프로젝트 키워드 역검색, 음향 기반 판정, 기관 관리 화면 전체.
- 만들지 않는 것: 퀴즈·카드 생성, 강사별 평가 점수, 취업 결과 연계.
- 기관 화면은 **제품이 직접 가진 데이터로만** 만든다. 출석 기록은 제품에 없다.

### 5-7. 뒤처짐 판정 기준 (T9)

- **복습 목록 화면을 연 날이 3일 연속 없으면** 뒤처지는 것으로 판정한다.
- 연속된 날은 **강의가 올라온 날만** 센다. 강의가 없는 날은 세지 않는다.
- 업로드 여부나 다시 듣기 비율을 판정 기준으로 쓰지 않는다.
- 기관 화면에는 며칠째 열지 않았는지를 그대로 보여주고, 점수나 등급으로 바꾸지 않는다.

### 5-8. 개인정보와 저작권 운영 규칙 (T7)

- 질의응답을 포함해 **전부 글로 옮긴다.** 강사 외 화자는 **"수강생" 한 가지로만** 표시하고 번호를 붙이지 않는다.
- 신호로 뽑는 대상은 **강사 발화뿐**이다. 수강생 발언은 맥락으로만 보이고 복습 목록에 올리지 않는다.
- 기관 화면에 전사 내용을 노출하지 않는다.
- 업로드 화면에서 질의응답이 함께 전사된다는 사실을 학습자에게 알린다.
- 전사본은 **과정 종료 후 6개월**까지 보관하고 그 뒤 삭제한다. 이 기간을 강사 동의서에 명시한다. 근거는 KDT 취업률이 훈련 종료 후 6개월 이후에 산정된다는 점이다.
- 원본 음성은 서버에 두지 않는다 (§5-3).
- 강사 동의서에 들어갈 항목: 녹음 허용 범위, 전사 대상, 보존 기간 6개월, 회차 단위 거부권.

### 5-9. 용어 표기 기준 (T8)

- 제품 문서 본문에는 **한국어 표기만** 쓴다. 영문 약어를 쓰지 않는다.
- 강사가 강조한 대목의 유형은 다음 여덟 가지로 부른다. 주의 · 실무 적용 · 핵심 원칙 · 요건 · 필수 행동 · 근거 · 견해 · 복습 대상.
- 요건은 **상태**를, 필수 행동은 **행동**을 가리킨다.
- 그 밖의 명칭은 풀어 쓴다. 신호는 **강사가 강조한 대목**, 앵커는 **해당 발화가 시작되는 지점**, 후보 풀은 **사람이 검토한 대상 목록**으로 쓴다. 가치 B라는 표기는 쓰지 않고 문장으로 풀어 쓴다.
- **코드에서 쓸 식별자는 PRD가 정하지 않는다.** SRS에서 위 한국어 표기와 짝지어 정의한다.

---

## 6. 기술 확정 사항

SRS 착수 전 결정으로 확정된 것이며, 이후 작업은 이 내용을 다시 협상하지 않는다. 결정의 근거는 [`docs/00-plan/product-decisions.md`](docs/00-plan/product-decisions.md) 2회차에 있다.

### 6-1. 실행 형태 (S1)

- 학습자는 **휴대폰 설치형 앱**, 훈련기관과 강사는 **웹 화면**을 쓴다.
- 학습자용 기능을 웹으로 만들지 않는다. 브라우저 저장소에 녹음 원본을 두지 않는다.
- 강사 동의는 설치 없이 웹에서 끝나야 한다. 강사에게 앱 설치를 요구하지 않는다.
- 앱 저장소의 용량을 관리하는 방법을 학습자에게 제공한다.
